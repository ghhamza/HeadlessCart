import { Route, Routes } from 'react-router-dom';

export default function Main() {
  return (
    <div style={{ height: '100%', width: '100%' }}>
      AA
      {/*<Routes>
        <Route key={'data-source'} element={<DataSource />} path="/data-source" />
        <Route key={'data-target'} element={<DataTarget />} path="/data-target" />
      </Routes>*/}
    </div>
  );
}
